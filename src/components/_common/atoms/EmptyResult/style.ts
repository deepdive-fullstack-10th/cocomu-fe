import styled from '@emotion/styled';

export const EmptyResultContainer = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  width: 25rem;
  user-select: none;

  img {
    max-width: 100%;
    height: auto;
  }
`;
