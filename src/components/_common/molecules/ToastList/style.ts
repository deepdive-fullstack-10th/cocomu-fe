import styled from '@emotion/styled';

const ListContainer = styled.div`
  position: fixed;
  top: 5%;
  right: 1%;
  z-index: 1000;

  display: flex;
  flex-direction: column;
  gap: 1.6rem;
`;

const S = {
  ListContainer,
};

export default S;
