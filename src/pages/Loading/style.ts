import styled from '@emotion/styled';

const LoadingContainer = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;

  height: 90vh;
`;

const S = {
  LoadingContainer,
};

export default S;
